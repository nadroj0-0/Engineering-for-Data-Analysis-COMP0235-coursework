---
# tasks for worker python environment
- name: Install Celery[redis]
  ansible.builtin.pip:
    name: "celery[redis]"
- name: Install Biopython for pipeline import
  ansible.builtin.pip:
    name: "biopython"
- name: Install Numpy for pipeline import
  ansible.builtin.pip:
    name: "numpy"
- name: Install Scipy for pipeline import
  ansible.builtin.pip:
    name: "scipy"
- name: Install Torch for pipeline import
  ansible.builtin.pip:
    name: "torch"
- name: Install MinIO Python client
  ansible.builtin.pip:
    name: minio
