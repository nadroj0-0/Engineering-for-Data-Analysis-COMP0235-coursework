# Bootstrap
This directory contains bootstrap files used to initialise the host VM
so it can provision the cluster from inside the cluster.

## Usage
After `terraform apply`, from this directory:

```bash
./bootstrap.sh
```
